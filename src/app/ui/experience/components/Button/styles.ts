import styled, { css } from 'styled-components';
import { StyledProps } from 'app/theme/types';

export const ButtonWrapper = styled.button(
  ({ theme }: StyledProps) => css`
    && {
      /* TODO: Write Button's styles */
    }
  `
);
